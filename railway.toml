[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && gunicorn app.wsgi --log-file -"
healthcheckPath = "/cars/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
